# Каталог для исходных открытых данных

В этот каталог можно поместить реальные открытые или обезличенные данные,
которые используются для расширения практикума.

Для занятий 1-3 базовые CSV-файлы формируются воспроизводимо в каталоге
`data/processed`.

Расширенные блокноты используют открытые источники с прямой загрузкой:

1. `zenodo_motor_temperature/` - исходные файлы Zenodo
   `ElectricMotorTemperature_TRAIN.ts` и `ElectricMotorTemperature_TEST.ts`;
2. `zenodo_pmsm_inverter_fault/` - архив и распакованные файлы набора
   диагностики PMSM-инвертора;
3. `mendeley_ev_powertrain_efficiency/` - архив и распакованные файлы набора
   данных по эффективности электропривода транспортного средства.

Для воспроизводимого заполнения этого каталога выполните:

```bash
source venv/bin/activate
python scripts/prepare_external_datasets.py
```

Kaggle API для расширенных материалов занятий 1-3 не требуется.
